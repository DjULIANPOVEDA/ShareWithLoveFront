export interface User {
  id: string;
  username: string;
  phone: string;
  email: string;
  address: string;
  token: string;
}
export interface UserAuth {
  userName: string;
  password: string;
}
